{
  "name": "@migo/store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Migo store: the on-chain purchase surface. A standalone React+Vite app served under /store on the same origin as the web client, so it shares the web client's IndexedDB (the session grant and the keystore snapshot) and opens purchases without a second sign-in.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@migo/sdk": "workspace:*",
    "@migo/wire": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.7",
    "@types/react-dom": "^19.0.3",
    "typescript": "^5.7.2"
  }
}
